import random

#die1 = random.randint(1,6) #randint gives a random integer that includes the 2 numbers and the numbers between the 2 numbers
#print(die1)

#die2 = random.randrange(6) #randrange gives a number between 0 and the number inside parenthesis
#print(die2)

die3 = random.randrange(1,8,2) #first 2 is the number between that includes them and the 3rd num is the increment
print(die3)